<%= render 'errors', locals: @item %>

<h1>Edit Item </h1>

<%= render 'form', locals: @item %>

<div class="btn-group">
  <p><%= button_to "Delete", item_path(@item), method: :delete, class: "btn btn-danger"%></p>
</div>
